import { multiply,makeLower } from "./assist";

test("makeLowertest", () => {
    expect(multiply(2,2)).toBe(4);
})

test("makeLowertest", () => {
    expect(makeLower("LETS CODE")).toBe("lets code")
})